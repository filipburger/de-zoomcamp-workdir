name: 'taxi_rides_ny'
version: '1.0.0'
profile: 'taxi_rides_ny'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

vars:
  payment_type_values: [1, 2, 3, 4, 5]

models:
  taxi_rides_ny:
    staging:
      +materialized: view
      +schema: staging
    core:
      +materialized: table
      +schema: core